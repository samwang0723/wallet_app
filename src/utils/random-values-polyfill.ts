// This import must come before any UUID usage
import 'react-native-get-random-values';

// This file doesn't export anything, it just needs to be imported
// to ensure the polyfill is loaded before any UUID usage
